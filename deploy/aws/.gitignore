# Terraform state (contains secrets)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# User-specific variables (contains IPs, external IDs)
terraform.tfvars

# Lambda zip artifacts
*.zip

# Generated SSH private keys
*.pem
