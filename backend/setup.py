from setuptools import setup, find_packages

setup(
    name="autonomy_backend",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies will be read from requirements.txt
    ],
    python_requires='>=3.8',
)
