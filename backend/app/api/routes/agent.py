# This file has been emptied. The /agent/suggest-orders endpoint it contained
# was Beer Game-specific (hardcoded 4-node NODES/NODE_INDEX topology) and was
# never registered as a router in main.py. Use the SC-compliant agent endpoints
# in app/api/endpoints/agent_scenario.py instead.
from fastapi import APIRouter

router = APIRouter()
