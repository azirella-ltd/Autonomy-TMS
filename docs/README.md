# Documentation Overview

The current `docs/` folder contains Markdown references that describe the supply-chain flow, complex configuration review notes, and debug log guidance. Additional reference PDFs live under `backend/llm_agent/resources/` (for example, Beer Game rules, calculations, and walkthroughs); they can be read alongside the Markdown files as long as their text content is accessible.
