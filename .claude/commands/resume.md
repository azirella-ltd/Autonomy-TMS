Read the session continuity file and recent git history to orient on where we left off:

1. Read `/home/trevor/.claude/projects/-home-trevor-Documents-Autonomy/memory/project_current_focus.md`
2. Run `git log --oneline -10` to see what was committed recently
3. Run `git status` to see any uncommitted work
4. Run `git diff --stat` to see any in-progress changes

Summarize concisely:
- **Last session**: What we were working on and key decisions
- **Uncommitted work**: Any changes in progress
- **Next steps**: What was queued up

Then ask: "Want to pick up where we left off, or work on something different?"
